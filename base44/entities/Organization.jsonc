{
  "name": "Organization",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Organization name e.g. Project YE PMO"
    },
    "slug": {
      "type": "string",
      "description": "Short URL-safe domain key for this client workspace e.g. 'acme'. Used to identify the domain a user logs into."
    },
    "org_type": {
      "type": "string",
      "enum": [
        "Internal",
        "Investor",
        "Engineering Consultant",
        "Legal Advisor",
        "Other"
      ],
      "description": "Organization type"
    },
    "logo": {
      "type": "string",
      "description": "Logo image URL"
    },
    "primary_contact_name": {
      "type": "string"
    },
    "primary_contact_email": {
      "type": "string"
    },
    "contract_reference": {
      "type": "string",
      "description": "NDA or contract number"
    },
    "access_start_date": {
      "type": "string",
      "format": "date"
    },
    "access_end_date": {
      "type": "string",
      "format": "date",
      "description": "Empty = indefinite"
    },
    "is_active": {
      "type": "boolean",
      "default": true
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "name",
    "org_type"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}